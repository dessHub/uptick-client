/* eslint-disable no-unused-vars */
export enum PaymentStatus {
    Unpaid = 'unpaid',
    InProgress = 'inProgress',
    Paid = 'paid'
};

export enum UserStatus {
    Active = 'active',
    Blocked = 'blocked'
}